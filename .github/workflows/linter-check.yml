name: hexlet-lint
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: 'Install_dependencies'
        run: | 
          pip install poetry
          make install
      - name: 'Run-linter'
        run: make lint